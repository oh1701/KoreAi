package devgyu.koreAi.domain.util

object TimeUtils {
    object MS {
        const val MS = 1L
        const val SEC = 1000L
        const val MIN = SEC * 60
        const val HOUR = MIN * 60
        const val DAY = HOUR * 60
    }

    object SEC {
        const val SEC = 1L
        const val MIN = SEC * 60
        const val HOUR = MIN * 60
        const val DAY = HOUR * 60
    }
}